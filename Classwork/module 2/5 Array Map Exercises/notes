higher order functions
map() example from scrimba video:
nate jensen:

    // Higher order array methods
    // .map()

        const arr = [1, 2, 3, 4, 5, 6, 7, 8]

        const result = arr.map(function(num){
            return num + 10
        })

        console.log(result)

        also--- as arrow function
        const result = arr.map(num => num + 10)

